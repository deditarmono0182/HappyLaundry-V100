import{FormEvent,useState}from'react'
import{Navigate}from'react-router-dom'
import{useAuth}from'../lib/auth'
import{isSupabaseConfigured}from'../lib/supabase'
export function LoginPage(){const{session,signIn}=useAuth();const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);if(session)return <Navigate to="/" replace/>;const submit=async(e:FormEvent)=>{e.preventDefault();setError('');setBusy(true);try{await signIn(email,password)}catch(r){setError(r instanceof Error?r.message:'Login gagal.')}finally{setBusy(false)}};return <div className="login-page"><div className="login-card"><img src="/logo-happylaundry.jpg" className="login-logo"/><span className="eyebrow">HAPPYLAUNDRY BABAKAN</span><h1>Masuk ke V100</h1><p>Gunakan akun owner, kasir, atau karyawan.</p>{!isSupabaseConfigured&&<div className="warning-box">Supabase belum dikonfigurasi.</div>}<form onSubmit={submit}><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label>{error&&<div className="error-box">{error}</div>}<button disabled={busy||!isSupabaseConfigured}>{busy?'Memproses...':'Masuk'}</button></form></div></div>}
